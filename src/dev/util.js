export function pickImages(count=3)
{
//	const sampleImages = [
////		'http://127.0.0.1/DSC00104.jpg'
//	];
//	let images = Object.assign([], sampleImages);
//	let result = [];
//
//	(function get() {
//		if (count <= 0) return;
//		result.push(images.splice(Math.floor(Math.random() * images.length), 1)[0]);
//		count--;
//		get();
//		if (count >= 1) get();
//	})();

	return null;
}